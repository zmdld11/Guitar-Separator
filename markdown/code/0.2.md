# 代码文档

生成时间: 2025-12-04 10:17:20
1. 输出的音量很轻，输出音质很差，有很多噪音
1. 从听感上来说这个模型只引入了噪音，没有成功将吉他音频提取出来，或者说将其他乐器的声音变小。

# src

## config.py
文件路径: src\config.py

```py
import torch
import os
from pathlib import Path

class Config:
    def __init__(self):
        # 获取项目根目录
        project_root = Path(__file__).parent.parent
        
        # 数据配置 - 使用绝对路径
        self.data_dir = str(project_root / "data")
        self.moisesdb_dir = str(project_root / "data" / "moisesdb_v0.1")
        self.medleydb_dir = str(project_root / "data" / "MedleyDB")
        self.sample_rate = 22050
        self.duration = 4.0
        
        # 模型配置
        self.n_fft = 1024
        self.hop_length = 256
        self.n_mels = 128
        self.audio_channels = 2
        
        # 训练配置
        self.batch_size = 2
        self.num_workers = 2
        self.learning_rate = 1e-4
        self.epochs = 100
        self.device = "cuda" if torch.cuda.is_available() else "cpu"
        
        # U-Net配置
        self.unet_channels = [32, 64, 128, 256]
        self.transformer_dim = 256
        self.num_heads = 8
        self.num_layers = 4
        
        # 路径配置
        self.model_save_path = "model/guitar_separator.pth"
        self.log_path = "output/log.txt"
        
        # 创建必要目录
        (project_root / "model").mkdir(exist_ok=True)
        (project_root / "output").mkdir(exist_ok=True)
        (project_root / "music").mkdir(exist_ok=True)
        (project_root / "data").mkdir(exist_ok=True)

config = Config()
```

## dataset.py
文件路径: src\dataset.py

```py
import os
import torch
import torchaudio
import numpy as np
from torch.utils.data import Dataset, DataLoader
import json
import random
from pathlib import Path
import yaml
from tqdm import tqdm
import warnings
import re
import hashlib
import pickle

class GuitarSeparationDataset(Dataset):
    def __init__(self, config, mode="train", use_cache=True):
        self.config = config
        self.mode = mode
        self.sample_rate = config.sample_rate
        self.duration = config.duration
        
        # 确保样本数是hop_length的倍数，避免STFT尺寸问题
        target_samples = int(self.duration * self.sample_rate)
        # 调整到最近的hop_length倍数
        self.samples_per_clip = (target_samples // config.hop_length) * config.hop_length
        
        print(f"音频片段长度: {self.samples_per_clip} 样本 ({self.samples_per_clip/self.sample_rate:.2f} 秒)")
        
        # 修复缓存路径
        project_root = Path(__file__).parent.parent
        self.cache_dir = project_root / "data" / "cache"
        self.cache_dir.mkdir(parents=True, exist_ok=True)
        self.cache_file = self.cache_dir / f"dataset_cache_{mode}.pkl"
        
        print("正在加载数据集...")
        
        if use_cache and self.cache_file.exists():
            print("从缓存加载数据集...")
            self.samples = self._load_from_cache()
        else:
            self.samples = self._load_samples()
            if use_cache:
                self._save_to_cache()
        
        if len(self.samples) == 0:
            warnings.warn("没有找到任何样本，请检查数据集路径")
        else:
            print(f"成功加载 {len(self.samples)} 个样本")
    
    def _load_from_cache(self):
        """从缓存加载数据"""
        try:
            with open(self.cache_file, 'rb') as f:
                cache_data = pickle.load(f)
            
            if self._validate_cache(cache_data):
                print("缓存验证成功")
                return cache_data['samples']
            else:
                print("缓存已过期，重新扫描数据集")
                return self._load_samples()
        except Exception as e:
            print(f"加载缓存失败: {e}，重新扫描数据集")
            return self._load_samples()
    
    def _save_to_cache(self):
        """保存数据到缓存"""
        try:
            cache_data = {
                'samples': self.samples,
                'config_hash': self._get_config_hash(),
                'dataset_modified': self._get_dataset_modified_time()
            }
            with open(self.cache_file, 'wb') as f:
                pickle.dump(cache_data, f)
            print(f"数据集缓存已保存: {self.cache_file}")
        except Exception as e:
            print(f"保存缓存失败: {e}")
    
    def _validate_cache(self, cache_data):
        """验证缓存是否有效"""
        if cache_data.get('config_hash') != self._get_config_hash():
            return False
        
        cached_modified = cache_data.get('dataset_modified', {})
        current_modified = self._get_dataset_modified_time()
        
        for dataset, modified_time in current_modified.items():
            if cached_modified.get(dataset) != modified_time:
                return False
        
        return True
    
    def _get_config_hash(self):
        """获取配置的哈希值"""
        config_str = f"{self.sample_rate}_{self.duration}_{self.config.moisesdb_dir}_{self.config.medleydb_dir}"
        return hashlib.md5(config_str.encode()).hexdigest()
    
    def _get_dataset_modified_time(self):
        """获取数据集的最后修改时间"""
        modified_times = {}
        
        moises_path = Path(self.config.moisesdb_dir)
        if moises_path.exists():
            try:
                all_files = [f for f in moises_path.rglob("*") if f.is_file()]
                if all_files:
                    latest_moises = max(f.stat().st_mtime for f in all_files)
                    modified_times['moisesdb'] = latest_moises
                else:
                    modified_times['moisesdb'] = 0
            except Exception as e:
                print(f"获取MoisesDB修改时间出错: {e}")
                modified_times['moisesdb'] = 0
        
        medley_path = Path(self.config.medleydb_dir)
        if medley_path.exists():
            try:
                all_files = [f for f in medley_path.rglob("*") if f.is_file()]
                if all_files:
                    latest_medley = max(f.stat().st_mtime for f in all_files)
                    modified_times['medleydb'] = latest_medley
                else:
                    modified_times['medleydb'] = 0
            except Exception as e:
                print(f"获取MedleyDB修改时间出错: {e}")
                modified_times['medleydb'] = 0
        
        return modified_times
    
    def _load_moisesdb_samples(self):
        """加载MoisesDB数据集样本"""
        samples = []
        moises_path = Path(self.config.moisesdb_dir)
        
        if not moises_path.exists():
            print(f"警告: MoisesDB路径不存在: {moises_path}")
            return samples
        
        print("正在扫描MoisesDB数据集...")
        item_dirs = [d for d in moises_path.iterdir() if d.is_dir()]
        
        for item_dir in tqdm(item_dirs, desc="MoisesDB"):
            try:
                data_json_path = item_dir / "data.json"
                if not data_json_path.exists():
                    continue
                
                with open(data_json_path, 'r', encoding='utf-8') as f:
                    data_info = json.load(f)
                
                guitar_dir = item_dir / "guitar"
                if guitar_dir.exists() and guitar_dir.is_dir():
                    mix_audio_path = self._create_mix_from_stems(item_dir, data_info)
                    if mix_audio_path is not None:
                        guitar_files = list(guitar_dir.glob("*.wav"))
                        for guitar_file in guitar_files:
                            if guitar_file.exists():
                                samples.append({
                                    'mix': str(mix_audio_path),
                                    'guitar': str(guitar_file),
                                    'is_temp_mix': True
                                })
            except Exception as e:
                print(f"加载MoisesDB样本时出错 {item_dir}: {e}")
                continue
        
        print(f"从MoisesDB加载了 {len(samples)} 个样本")
        return samples
    
    def _create_mix_from_stems(self, item_dir, data_info):
        """从各个音轨创建混合音轨"""
        temp_mix_path = item_dir / "temp_mix.wav"
        
        if temp_mix_path.exists() and temp_mix_path.stat().st_size > 1000:
            return temp_mix_path
            
        try:
            all_stems = []
            stem_dirs = ['guitar', 'bass', 'drums', 'vocals', 'piano', 'other']
            
            for stem_dir in stem_dirs:
                stem_path = item_dir / stem_dir
                if stem_path.exists() and stem_path.is_dir():
                    for stem_file in stem_path.glob("*.wav"):
                        try:
                            audio, sr = torchaudio.load(stem_file)
                            if sr != self.sample_rate:
                                resampler = torchaudio.transforms.Resample(sr, self.sample_rate)
                                audio = resampler(audio)
                            all_stems.append(audio)
                        except Exception as e:
                            print(f"加载音轨 {stem_file} 时出错: {e}")
                            continue
            
            if len(all_stems) == 0:
                print(f"警告: {item_dir} 中没有找到有效的音轨文件")
                return None
            
            max_length = max(stem.shape[1] for stem in all_stems)
            
            mix_audio = torch.zeros((2, max_length))
            for stem in all_stems:
                if stem.shape[0] == 1:
                    stem = stem.repeat(2, 1)
                elif stem.shape[0] > 2:
                    stem = stem[:2, :]
                
                if stem.shape[1] < max_length:
                    padded_stem = torch.zeros((2, max_length))
                    padded_stem[:, :stem.shape[1]] = stem
                    stem = padded_stem
                
                mix_audio += stem
            
            max_val = mix_audio.abs().max()
            if max_val > 0:
                mix_audio = mix_audio / (max_val * 1.1)
            
            torchaudio.save(str(temp_mix_path), mix_audio, self.sample_rate)
            print(f"创建混合文件: {temp_mix_path}")
            
            return temp_mix_path
            
        except Exception as e:
            print(f"创建混合音轨时出错 {item_dir}: {e}")
            return None
    
    def _load_medleydb_samples(self):
        """加载MedleyDB数据集样本"""
        samples = []
        medley_path = Path(self.config.medleydb_dir)
        
        if not medley_path.exists():
            print(f"警告: MedleyDB路径不存在: {medley_path}")
            return samples
        
        print("正在扫描MedleyDB数据集...")
        track_dirs = [d for d in medley_path.iterdir() if d.is_dir()]
        
        for track_dir in tqdm(track_dirs, desc="MedleyDB"):
            try:
                mix_files = list(track_dir.glob("*_MIX.wav"))
                if not mix_files:
                    continue
                    
                mix_path = mix_files[0]
                stems_dir = track_dir / f"{track_dir.name}_STEMS"
                
                if not stems_dir.exists():
                    continue
                    
                for stem_file in stems_dir.glob("*.wav"):
                    if self._is_guitar_stem(stem_file.name):
                        samples.append({
                            'mix': str(mix_path),
                            'guitar': str(stem_file),
                            'is_temp_mix': False
                        })
            except Exception as e:
                print(f"处理MedleyDB目录 {track_dir} 时出错: {e}")
                continue
        
        print(f"从MedleyDB加载了 {len(samples)} 个样本")
        return samples
    
    def _is_guitar_stem(self, filename):
        """识别吉他音轨"""
        guitar_keywords = [
            'guitar', 'gtr', 'git', 'guitare', 'guitarra', 
            'acoustic', 'electric', 'nylon', 'steel'
        ]
        anti_keywords = ['bass', 'kick', 'snare', 'drum', 'vocal', 'voice']
        
        filename_lower = filename.lower()
        
        for anti in anti_keywords:
            if anti in filename_lower:
                return False
        
        for keyword in guitar_keywords:
            if keyword in filename_lower:
                return True
        
        stem_match = re.search(r'STEM_(\d+)', filename)
        if stem_match:
            stem_num = int(stem_match.group(1))
            if stem_num <= 4:
                return True
        
        return False
    
    def _load_samples(self):
        """加载所有样本"""
        samples = []
        
        moises_samples = self._load_moisesdb_samples()
        samples.extend(moises_samples)
        
        medley_samples = self._load_medleydb_samples()
        samples.extend(medley_samples)
        
        if len(samples) == 0:
            print("创建测试样本...")
            samples = self._create_test_samples()
        
        return samples
    
    def _create_test_samples(self):
        """创建测试样本（用于调试）"""
        test_samples = []
        project_root = Path(__file__).parent.parent
        test_dir = project_root / "test_samples"
        test_dir.mkdir(exist_ok=True)
        
        print("正在生成测试音频...")
        for i in tqdm(range(20), desc="生成测试样本"):
            t = torch.linspace(0, self.duration, int(self.sample_rate * self.duration))
            
            mix_freqs = [220, 440, 880, 1320]
            mix_audio = torch.zeros(2, len(t))
            for freq in mix_freqs:
                wave = torch.sin(2 * np.pi * freq * t)
                mix_audio += wave.unsqueeze(0).repeat(2, 1) * 0.2
            
            guitar_freqs = [220, 440, 660]
            guitar_audio = torch.zeros(2, len(t))
            for freq in guitar_freqs:
                wave = torch.sin(2 * np.pi * freq * t)
                guitar_audio += wave.unsqueeze(0).repeat(2, 1) * 0.4
            
            envelope = torch.ones_like(t)
            attack = int(0.1 * len(t))
            release = int(0.2 * len(t))
            envelope[:attack] = torch.linspace(0, 1, attack)
            envelope[-release:] = torch.linspace(1, 0, release)
            
            mix_audio *= envelope
            guitar_audio *= envelope
            
            mix_path = test_dir / f"test_mix_{i:02d}.wav"
            guitar_path = test_dir / f"test_guitar_{i:02d}.wav"
            
            torchaudio.save(str(mix_path), mix_audio, self.sample_rate)
            torchaudio.save(str(guitar_path), guitar_audio, self.sample_rate)
            
            test_samples.append({
                'mix': str(mix_path),
                'guitar': str(guitar_path),
                'is_temp_mix': False
            })
        
        print(f"创建了 {len(test_samples)} 个测试样本")
        return test_samples
    
    def _load_audio(self, path):
        try:
            audio, sr = torchaudio.load(path)
            if sr != self.sample_rate:
                resampler = torchaudio.transforms.Resample(sr, self.sample_rate)
                audio = resampler(audio)
            
            if audio.shape[0] == 1:
                audio = audio.repeat(2, 1)
            elif audio.shape[0] > 2:
                audio = audio[:2, :]
                
            return audio
        except Exception as e:
            print(f"加载音频出错 {path}: {e}")
            return torch.zeros((2, self.samples_per_clip))
    
    def __len__(self):
        return len(self.samples)
    
    def __getitem__(self, idx):
        sample = self.samples[idx]
        
        mix_audio = self._load_audio(sample['mix'])
        guitar_audio = self._load_audio(sample['guitar'])
        
        min_length = min(mix_audio.shape[1], guitar_audio.shape[1])
        mix_audio = mix_audio[:, :min_length]
        guitar_audio = guitar_audio[:, :min_length]
        
        # 确保长度是hop_length的倍数
        if mix_audio.shape[1] > self.samples_per_clip:
            # 裁剪到目标长度（确保是hop_length的倍数）
            cropped_length = (self.samples_per_clip // self.config.hop_length) * self.config.hop_length
            start = random.randint(0, mix_audio.shape[1] - cropped_length)
            mix_audio = mix_audio[:, start:start + cropped_length]
            guitar_audio = guitar_audio[:, start:start + cropped_length]
        elif mix_audio.shape[1] < self.samples_per_clip:
            # 填充到目标长度（确保是hop_length的倍数）
            padded_length = (self.samples_per_clip // self.config.hop_length) * self.config.hop_length
            padded_mix = torch.zeros((2, padded_length))
            padded_guitar = torch.zeros((2, padded_length))
            padded_mix[:, :mix_audio.shape[1]] = mix_audio
            padded_guitar[:, :guitar_audio.shape[1]] = guitar_audio
            mix_audio = padded_mix
            guitar_audio = padded_guitar
        
        if self.mode == "train":
            gain = random.uniform(0.7, 1.3)
            mix_audio = mix_audio * gain
            guitar_audio = guitar_audio * gain
            
            if random.random() > 0.5:
                mix_audio = -mix_audio
                guitar_audio = -guitar_audio
            
            if random.random() > 0.8:
                noise_level = random.uniform(0.001, 0.01)
                mix_audio += torch.randn_like(mix_audio) * noise_level
        
        return {
            'mix': mix_audio,
            'guitar': guitar_audio
        }

def get_dataloaders(config):
    print("初始化数据加载器...")
    dataset = GuitarSeparationDataset(config, mode="train", use_cache=True)
    
    if len(dataset) == 0:
        raise ValueError("没有找到任何训练样本，请检查数据集路径")
    
    train_size = int(0.8 * len(dataset))
    val_size = len(dataset) - train_size
    
    if train_size == 0 or val_size == 0:
        train_dataset = dataset
        val_dataset = dataset
        print("使用全部数据进行训练和验证")
    else:
        train_dataset, val_dataset = torch.utils.data.random_split(
            dataset, [train_size, val_size]
        )
        print(f"训练集: {len(train_dataset)} 样本, 验证集: {len(val_dataset)} 样本")
    
    train_loader = DataLoader(
        train_dataset,
        batch_size=config.batch_size,
        shuffle=True,
        num_workers=min(config.num_workers, 4),
        pin_memory=True
    )
    
    val_loader = DataLoader(
        val_dataset,
        batch_size=config.batch_size,
        shuffle=False,
        num_workers=min(config.num_workers, 4),
        pin_memory=True
    )
    
    return train_loader, val_loader
```

## model.py
文件路径: src\model.py

```py
import torch
import torch.nn as nn
import torch.nn.functional as F
import numpy as np

class FrequencyUNet(nn.Module):
    def __init__(self, config):
        super().__init__()
        self.config = config
        
        # STFT参数
        self.n_fft = config.n_fft
        self.hop_length = config.hop_length
        self.register_buffer('window', torch.hann_window(self.n_fft))
        
        channels = config.unet_channels
        
        # 编码器
        self.encoders = nn.ModuleList()
        for i in range(len(channels)):
            in_ch = 4 if i == 0 else channels[i-1]  # 4个通道：实部1，实部2，虚部1，虚部2
            out_ch = channels[i]
            
            encoder = nn.Sequential(
                nn.Conv2d(in_ch, out_ch, 3, padding=1),
                nn.BatchNorm2d(out_ch),
                nn.ReLU(),
                nn.Conv2d(out_ch, out_ch, 3, padding=1),
                nn.BatchNorm2d(out_ch),
                nn.ReLU()
            )
            self.encoders.append(encoder)
        
        # 下采样层
        self.downsample_layers = nn.ModuleList([
            nn.MaxPool2d(2) for _ in range(len(channels) - 1)
        ])
        
        # 解码器
        self.decoders = nn.ModuleList()
        for i in range(len(channels)-1, 0, -1):
            in_ch = channels[i] + channels[i-1]  # 跳跃连接
            out_ch = channels[i-1]
            
            decoder = nn.Sequential(
                nn.Conv2d(in_ch, out_ch, 3, padding=1),
                nn.BatchNorm2d(out_ch),
                nn.ReLU(),
                nn.Conv2d(out_ch, out_ch, 3, padding=1),
                nn.BatchNorm2d(out_ch),
                nn.ReLU()
            )
            self.decoders.append(decoder)
        
        # 上采样层
        self.upsample_layers = nn.ModuleList([
            nn.Upsample(scale_factor=2, mode='bilinear', align_corners=False) 
            for _ in range(len(channels) - 1)
        ])
        
        # 输出层 - 预测吉他掩码
        self.output = nn.Conv2d(channels[0], 2, 1)  # 2个通道：实部掩码，虚部掩码
    
    def forward(self, x):
        batch_size, audio_channels, samples = x.shape
        
        # 计算STFT
        spec_list = []
        for i in range(audio_channels):
            channel_spec = torch.stft(x[:, i], n_fft=self.n_fft, hop_length=self.hop_length,
                                    window=self.window, return_complex=True)
            spec_list.append(channel_spec)
        
        # 合并通道 [batch, 2, freq, time]
        spec_real = torch.stack([s.real for s in spec_list], dim=1)
        spec_imag = torch.stack([s.imag for s in spec_list], dim=1)
        
        # 输入特征 [batch, 4, freq, time]
        freq_input = torch.cat([spec_real, spec_imag], dim=1)
        
        # 编码路径
        skips = []
        current = freq_input
        
        for i, encoder in enumerate(self.encoders):
            current = encoder(current)
            skips.append(current)
            
            # 下采样（除了最后一层）
            if i < len(self.downsample_layers):
                current = self.downsample_layers[i](current)
        
        # 解码路径
        for i, (upsample, decoder) in enumerate(zip(self.upsample_layers, self.decoders)):
            # 上采样
            current = upsample(current)
            
            # 获取跳跃连接
            skip = skips[-(i+2)]
            
            # 调整尺寸以匹配
            if current.shape[2:] != skip.shape[2:]:
                current = F.interpolate(current, size=skip.shape[2:], mode='bilinear', align_corners=False)
            
            # 连接跳跃连接
            current = torch.cat([current, skip], dim=1)
            
            # 解码
            current = decoder(current)
        
        # 预测掩码
        mask = self.output(current)
        mask_real = mask[:, 0:1]  # [batch, 1, freq, time]
        mask_imag = mask[:, 1:2]  # [batch, 1, freq, time]
        
        # 应用掩码到原始频谱
        # 注意：这里我们只对第一个音频通道应用掩码，因为我们想要分离吉他
        output_spec_real = spec_real[:, 0:1] * mask_real - spec_imag[:, 0:1] * mask_imag
        output_spec_imag = spec_real[:, 0:1] * mask_imag + spec_imag[:, 0:1] * mask_real
        
        # 合并为复数频谱 [batch, 1, freq, time]
        output_spec = torch.complex(output_spec_real, output_spec_imag)
        
        # ISTFT恢复时域信号 - 注意维度处理
        output_wav_list = []
        for i in range(output_spec.shape[0]):
            # 对每个批次单独处理
            single_spec = output_spec[i, 0]  # [freq, time]
            single_wav = torch.istft(single_spec, n_fft=self.n_fft, hop_length=self.hop_length,
                                   length=samples, window=self.window)
            output_wav_list.append(single_wav)
        
        # 堆叠结果 [batch, samples]
        output_wav = torch.stack(output_wav_list, dim=0)
        
        return output_wav.unsqueeze(1)  # [batch, 1, samples]

class GuitarSeparator(nn.Module):
    def __init__(self, config):
        super().__init__()
        self.freq_unet = FrequencyUNet(config)
        
    def forward(self, mix):
        return self.freq_unet(mix)
```

## train.py
文件路径: src\train.py

```py
import torch
import torch.nn as nn
from torch.optim import Adam
from torch.optim.lr_scheduler import ReduceLROnPlateau
import torch.nn.functional as F
import time
import os
from pathlib import Path
from tqdm import tqdm

from config import Config
from dataset import get_dataloaders
from model import GuitarSeparator

class Trainer:
    def __init__(self, config):
        self.config = config
        self.device = config.device
        
        # 创建目录
        project_root = Path(__file__).parent.parent
        (project_root / "model").mkdir(exist_ok=True)
        (project_root / "output").mkdir(exist_ok=True)
        
        print("正在初始化模型...")
        # 初始化模型
        self.model = GuitarSeparator(config).to(self.device)
        
        # 打印模型参数数量
        total_params = sum(p.numel() for p in self.model.parameters())
        print(f"模型参数总数: {total_params:,}")
        
        # 优化器
        self.optimizer = Adam(self.model.parameters(), lr=config.learning_rate)
        self.scheduler = ReduceLROnPlateau(self.optimizer, mode='min', factor=0.5, patience=5, verbose=True)
        
        # 损失函数
        self.l1_loss = nn.L1Loss()
        self.mse_loss = nn.MSELoss()
        
        # 数据加载器
        self.train_loader, self.val_loader = get_dataloaders(config)
        
        # 训练状态
        self.best_val_loss = float('inf')
        self.train_losses = []
        self.val_losses = []
        
    def compute_loss(self, output, target):
        # 确保输出和目标形状一致
        # 如果目标是立体声，取平均值转为单声道
        if target.shape[1] == 2 and output.shape[1] == 1:
            target_mono = target.mean(dim=1, keepdim=True)  # 立体声转单声道
        else:
            target_mono = target
        
        # 基础时域损失
        l1_loss = self.l1_loss(output, target_mono)
        mse_loss = self.mse_loss(output, target_mono)
        
        # 频谱一致性损失 - 修复维度问题
        try:
            # 确保输入是2D [batch, samples]
            output_2d = output.squeeze(1)  # 移除通道维度 [batch, samples]
            target_2d = target_mono.squeeze(1)  # 移除通道维度 [batch, samples]
            
            output_spec = torch.stft(output_2d, n_fft=self.config.n_fft, 
                                hop_length=self.config.hop_length, 
                                return_complex=True, 
                                window=torch.hann_window(self.config.n_fft).to(output.device))
            target_spec = torch.stft(target_2d, n_fft=self.config.n_fft, 
                                hop_length=self.config.hop_length, 
                                return_complex=True, 
                                window=torch.hann_window(self.config.n_fft).to(target.device))
            
            magnitude_loss = F.l1_loss(output_spec.abs(), target_spec.abs())
            spec_loss = magnitude_loss
        except Exception as e:
            print(f"频谱损失计算失败: {e}")
            spec_loss = torch.tensor(0.0, device=output.device)
        
        # 总损失
        total_loss = l1_loss + 0.5 * mse_loss + 0.2 * spec_loss
        return total_loss
    
    def train_epoch(self, epoch):
        self.model.train()
        total_loss = 0
        num_batches = len(self.train_loader)
        
        progress_bar = tqdm(self.train_loader, desc=f'训练 Epoch {epoch}')
        
        for batch_idx, batch in enumerate(progress_bar):
            mix = batch['mix'].to(self.device)
            guitar = batch['guitar'].to(self.device)
            
            self.optimizer.zero_grad()
            
            try:
                output = self.model(mix)
                loss = self.compute_loss(output, guitar)
                
                loss.backward()
                torch.nn.utils.clip_grad_norm_(self.model.parameters(), max_norm=1.0)
                self.optimizer.step()
                
                total_loss += loss.item()
                
                # 更新进度条
                progress_bar.set_postfix({
                    'Loss': f'{loss.item():.6f}',
                    'Avg Loss': f'{total_loss / (batch_idx + 1):.6f}'
                })
            except Exception as e:
                print(f"训练批次 {batch_idx} 时出错: {e}")
                continue
        
        return total_loss / num_batches if num_batches > 0 else float('inf')
    
    def validate(self, epoch):
        self.model.eval()
        total_loss = 0
        num_batches = len(self.val_loader)
        
        progress_bar = tqdm(self.val_loader, desc=f'验证 Epoch {epoch}')
        
        with torch.no_grad():
            for batch_idx, batch in enumerate(progress_bar):
                mix = batch['mix'].to(self.device)
                guitar = batch['guitar'].to(self.device)
                
                try:
                    output = self.model(mix)
                    loss = self.compute_loss(output, guitar)
                    
                    total_loss += loss.item()
                    
                    # 更新进度条
                    progress_bar.set_postfix({
                        'Loss': f'{loss.item():.6f}',
                        'Avg Loss': f'{total_loss / (batch_idx + 1):.6f}'
                    })
                except Exception as e:
                    print(f"验证批次 {batch_idx} 时出错: {e}")
                    continue
        
        return total_loss / num_batches if num_batches > 0 else float('inf')
    
    def save_model(self, path):
        project_root = Path(__file__).parent.parent
        full_path = project_root / path
        full_path.parent.mkdir(parents=True, exist_ok=True)
        
        torch.save({
            'model_state_dict': self.model.state_dict(),
            'optimizer_state_dict': self.optimizer.state_dict(),
            'best_val_loss': self.best_val_loss,
            'train_losses': self.train_losses,
            'val_losses': self.val_losses,
            'config': self.config.__dict__
        }, str(full_path))
    
    def load_model(self, path):
        project_root = Path(__file__).parent.parent
        full_path = project_root / path
        checkpoint = torch.load(full_path, map_location=self.device)
        self.model.load_state_dict(checkpoint['model_state_dict'])
        self.optimizer.load_state_dict(checkpoint['optimizer_state_dict'])
        self.best_val_loss = checkpoint['best_val_loss']
        self.train_losses = checkpoint['train_losses']
        self.val_losses = checkpoint['val_losses']
    
    def log(self, message):
        print(message)
        project_root = Path(__file__).parent.parent
        log_path = project_root / self.config.log_path
        log_path.parent.mkdir(parents=True, exist_ok=True)
        with open(log_path, 'a', encoding='utf-8') as f:
            f.write(message + '\n')
    
    def train(self):
        self.log("开始训练吉他音轨分离模型...")
        self.log(f"设备: {self.device}")
        self.log(f"训练样本数: {len(self.train_loader.dataset)}")
        self.log(f"验证样本数: {len(self.val_loader.dataset)}")
        self.log(f"批次大小: {self.config.batch_size}")
        self.log(f"学习率: {self.config.learning_rate}")
        
        for epoch in range(1, self.config.epochs + 1):
            start_time = time.time()
            
            # 训练
            train_loss = self.train_epoch(epoch)
            
            # 验证
            val_loss = self.validate(epoch)
            
            # 学习率调度
            self.scheduler.step(val_loss)
            
            # 记录损失
            self.train_losses.append(train_loss)
            self.val_losses.append(val_loss)
            
            epoch_time = time.time() - start_time
            
            # 日志
            log_message = (f"Epoch {epoch}/{self.config.epochs}, "
                         f"Train Loss: {train_loss:.6f}, Val Loss: {val_loss:.6f}, "
                         f"Time: {epoch_time:.2f}s, LR: {self.optimizer.param_groups[0]['lr']:.2e}")
            self.log(log_message)
            
            # 保存最佳模型
            if val_loss < self.best_val_loss:
                self.best_val_loss = val_loss
                self.save_model(self.config.model_save_path)
                self.log(f"保存最佳模型，验证损失: {val_loss:.6f}")
            
            # 每10个epoch保存一次检查点
            if epoch % 10 == 0:
                checkpoint_path = f"model/checkpoint_epoch_{epoch}.pth"
                self.save_model(checkpoint_path)
                self.log(f"保存检查点: {checkpoint_path}")

if __name__ == "__main__":
    config = Config()
    trainer = Trainer(config)
    trainer.train()
```

## __init__.py
文件路径: src\__init__.py

```py

```

# test

## debug_dataset.py
文件路径: test\debug_dataset.py

```py
import sys
import os
from pathlib import Path

# 添加src目录到路径
sys.path.append('../src')

from config import Config
from dataset import GuitarSeparationDataset

def debug_moisesdb():
    """调试MoisesDB数据加载"""
    config = Config()
    
    print("MoisesDB路径:", config.moisesdb_dir)
    print("路径存在:", os.path.exists(config.moisesdb_dir))
    
    if os.path.exists(config.moisesdb_dir):
        moises_path = Path(config.moisesdb_dir)
        item_dirs = [d for d in moises_path.iterdir() if d.is_dir()]
        print(f"找到 {len(item_dirs)} 个歌曲目录")
        
        for i, item_dir in enumerate(item_dirs[:3]):  # 只检查前3个
            print(f"\n--- 检查目录 {i+1}: {item_dir.name} ---")
            
            # 检查data.json
            data_json = item_dir / "data.json"
            print(f"data.json存在: {data_json.exists()}")
            
            if data_json.exists():
                import json
                try:
                    with open(data_json, 'r', encoding='utf-8') as f:
                        data = json.load(f)
                    print(f"歌曲: {data.get('song', 'N/A')}")
                    print(f"艺术家: {data.get('artist', 'N/A')}")
                except Exception as e:
                    print(f"读取data.json出错: {e}")
            
            # 检查乐器目录
            instrument_dirs = ['guitar', 'bass', 'drums', 'vocals', 'piano', 'other']
            for inst in instrument_dirs:
                inst_dir = item_dir / inst
                if inst_dir.exists():
                    wav_files = list(inst_dir.glob("*.wav"))
                    print(f"{inst}: {len(wav_files)} 个文件")
            
            # 检查是否有temp_mix.wav
            mix_file = item_dir / "temp_mix.wav"
            print(f"temp_mix.wav存在: {mix_file.exists()}")

def debug_medleydb():
    """调试MedleyDB数据加载"""
    config = Config()
    
    print("\nMedleyDB路径:", config.medleydb_dir)
    print("路径存在:", os.path.exists(config.medleydb_dir))
    
    if os.path.exists(config.medleydb_dir):
        medley_path = Path(config.medleydb_dir)
        track_dirs = [d for d in medley_path.iterdir() if d.is_dir()]
        print(f"找到 {len(track_dirs)} 个音轨目录")
        
        for i, track_dir in enumerate(track_dirs[:3]):  # 只检查前3个
            print(f"\n--- 检查目录 {i+1}: {track_dir.name} ---")
            
            # 检查混合文件
            mix_files = list(track_dir.glob("*_MIX.wav"))
            print(f"混合文件: {len(mix_files)} 个")
            if mix_files:
                print(f"混合文件路径: {mix_files[0]}")
            
            # 检查STEMS目录
            stems_dir = track_dir / f"{track_dir.name}_STEMS"
            print(f"STEMS目录存在: {stems_dir.exists()}")
            
            if stems_dir.exists():
                stem_files = list(stems_dir.glob("*.wav"))
                print(f"音轨文件: {len(stem_files)} 个")
                
                # 显示前几个音轨文件
                for j, stem_file in enumerate(stem_files[:5]):
                    print(f"  音轨 {j+1}: {stem_file.name}")

def debug_dataset():
    """调试数据集"""
    config = Config()
    
    # 清除缓存以强制重新扫描
    project_root = Path(__file__).parent.parent
    cache_dir = project_root / "data" / "cache"
    if cache_dir.exists():
        for cache_file in cache_dir.glob("*.pkl"):
            cache_file.unlink()
        print("已清除缓存")
    
    print("创建数据集实例...")
    dataset = GuitarSeparationDataset(config, use_cache=False)
    
    print(f"\n数据集大小: {len(dataset)}")
    
    if len(dataset) > 0:
        sample = dataset[0]
        print(f"混合音频形状: {sample['mix'].shape}")
        print(f"吉他音频形状: {sample['guitar'].shape}")
        
        # 检查前几个样本
        for i in range(min(3, len(dataset))):
            sample = dataset[i]
            print(f"\n样本 {i}:")
            print(f"  混合: {dataset.samples[i]['mix']}")
            print(f"  吉他: {dataset.samples[i]['guitar']}")
            print(f"  形状: mix={sample['mix'].shape}, guitar={sample['guitar'].shape}")

def test_cache():
    """测试缓存功能"""
    config = Config()
    
    print("\n=== 测试缓存功能 ===")
    
    # 第一次加载（创建缓存）
    print("第一次加载数据集...")
    dataset1 = GuitarSeparationDataset(config, use_cache=True)
    print(f"第一次加载样本数: {len(dataset1.samples)}")
    
    # 第二次加载（使用缓存）
    print("第二次加载数据集...")
    dataset2 = GuitarSeparationDataset(config, use_cache=True)
    print(f"第二次加载样本数: {len(dataset2.samples)}")
    
    # 验证缓存是否工作
    if len(dataset1.samples) == len(dataset2.samples):
        print("缓存功能正常")
    else:
        print("缓存功能异常")

if __name__ == "__main__":
    print("=== 调试MoisesDB ===")
    debug_moisesdb()
    
    print("\n=== 调试MedleyDB ===")
    debug_medleydb()
    
    print("\n=== 调试数据集 ===")
    debug_dataset()
    
    print("\n=== 测试缓存 ===")
    test_cache()
```

## debug_model.py
文件路径: test\debug_model.py

```py
import sys
import os
from pathlib import Path
import torch

# 添加src目录到路径
sys.path.append('../src')

from config import Config
from model import GuitarSeparator

def debug_model():
    """调试模型前向传播"""
    config = Config()
    
    print("初始化模型...")
    model = GuitarSeparator(config).to(config.device)
    
    # 创建测试输入
    batch_size = 2
    audio_channels = 2
    samples = int(config.sample_rate * config.duration)
    
    print(f"创建测试输入: batch_size={batch_size}, channels={audio_channels}, samples={samples}")
    test_input = torch.randn(batch_size, audio_channels, samples).to(config.device)
    
    print("运行前向传播...")
    try:
        with torch.no_grad():
            output = model(test_input)
            print(f"输入形状: {test_input.shape}")
            print(f"输出形状: {output.shape}")
            print("模型前向传播成功!")
    except Exception as e:
        print(f"模型前向传播失败: {e}")
        import traceback
        traceback.print_exc()

def debug_stft():
    """调试STFT计算"""
    config = Config()
    
    # 创建测试音频
    samples = int(config.sample_rate * config.duration)
    test_audio = torch.randn(2, samples)
    
    print(f"音频形状: {test_audio.shape}")
    
    # 计算STFT
    spec = torch.stft(test_audio[0], n_fft=config.n_fft, hop_length=config.hop_length,
                     window=torch.hann_window(config.n_fft), return_complex=True)
    
    print(f"频谱形状: {spec.shape}")
    print(f"频点数: {config.n_fft // 2 + 1}")
    print(f"时间帧数: {(samples - config.n_fft) // config.hop_length + 1}")

if __name__ == "__main__":
    print("=== 调试STFT ===")
    debug_stft()
    
    print("\n=== 调试模型 ===")
    debug_model()
```

## inference.py
文件路径: test\inference.py

```py
import os
import sys
import torch
import torchaudio
import torch.nn.functional as F
import re
from pathlib import Path

# 添加项目根目录到路径
PROJECT_ROOT = Path(__file__).parent.parent
sys.path.append(str(PROJECT_ROOT))

from src.config import Config
from src.model import GuitarSeparator

# 创建配置实例
config = Config()

MUSIC_DIR = PROJECT_ROOT / 'music'
OUTPUT_DIR = PROJECT_ROOT / 'output'

def sanitize_filename(filename):
    """清理文件名，移除或替换非法字符"""
    # 移除或替换Windows/Linux文件系统不支持的字符
    filename = re.sub(r'[<>:"/\\|?*]', '_', filename)
    # 移除控制字符
    filename = re.sub(r'[\x00-\x1f\x7f]', '', filename)
    # 限制文件名长度（Windows最大255字符）
    if len(filename) > 200:
        name, ext = os.path.splitext(filename)
        filename = name[:200] + ext
    return filename

def list_audio_files(directory):
    """列出音频文件"""
    directory = Path(directory)
    if not directory.exists():
        directory.mkdir(parents=True, exist_ok=True)
        print(f"提示：已创建文件夹 '{directory}'")
        return []
        
    audio_extensions = ['.mp3', '.wav', '.flac', '.m4a', '.aac']
    audio_files = []
    for ext in audio_extensions:
        audio_files.extend(directory.glob(f"*{ext}"))
        audio_files.extend(directory.glob(f"*{ext.upper()}"))
    
    return [str(f) for f in audio_files]

def select_audio_file():
    """选择音频文件"""
    audio_files = list_audio_files(MUSIC_DIR)
    
    if not audio_files:
        print(f"未找到音频文件，请将音频文件放入 {MUSIC_DIR} 文件夹")
        return None
    
    print(f"\n发现 {len(audio_files)} 个音频文件：")
    for i, file in enumerate(audio_files, 1):
        filename = Path(file).name
        print(f"{i}. {filename}")
    
    while True:
        try:
            choice = input(f"\n请选择 (1-{len(audio_files)}): ")
            choice_idx = int(choice) - 1
            if 0 <= choice_idx < len(audio_files):
                return audio_files[choice_idx]
            else:
                print("无效选择")
        except ValueError:
            print("请输入数字")
        except KeyboardInterrupt:
            print("\n程序已退出")
            sys.exit(0)

def load_model():
    """加载模型"""
    print("正在加载模型...")
    
    # 创建模型实例
    model = GuitarSeparator(config).to(config.device)
    
    # 优先尝试加载最佳模型
    best_path = PROJECT_ROOT / 'model' / 'guitar_separator_best.pth'
    last_path = PROJECT_ROOT / 'model' / 'guitar_separator_last.pth'
    default_path = PROJECT_ROOT / 'model' / 'guitar_separator.pth'
    
    checkpoint_path = None
    if best_path.exists():
        checkpoint_path = best_path
        print(f"使用最佳模型: {best_path}")
    elif last_path.exists():
        checkpoint_path = last_path
        print(f"使用最新模型: {last_path}")
    elif default_path.exists():
        checkpoint_path = default_path
        print(f"使用默认模型: {default_path}")
    else:
        # 尝试找到任何模型文件
        model_files = list((PROJECT_ROOT / 'model').glob('*.pth'))
        if model_files:
            checkpoint_path = model_files[0]
            print(f"使用找到的模型: {checkpoint_path}")
        else:
            print("错误：未找到任何模型文件！")
            print(f"请确保模型文件保存在 {PROJECT_ROOT / 'model'} 目录中")
            return None

    try:
        checkpoint = torch.load(checkpoint_path, map_location=config.device)
        
        # 处理模型状态字典
        state_dict = checkpoint['model_state_dict']
        
        # 尝试加载模型
        model.load_state_dict(state_dict)
        model.eval()
        print(f"模型加载成功，设备: {config.device}")
        return model
    except Exception as e:
        print(f"模型加载失败: {e}")
        print("尝试修复模型状态字典...")
        
        try:
            # 尝试修复状态字典的键名
            new_state_dict = {}
            for key, value in state_dict.items():
                # 移除可能的额外前缀
                if key.startswith('model.'):
                    new_key = key[6:]  # 移除 'model.' 前缀
                elif key.startswith('module.'):
                    new_key = key[7:]  # 移除 'module.' 前缀
                else:
                    new_key = key
                new_state_dict[new_key] = value
            
            model.load_state_dict(new_state_dict, strict=False)
            model.eval()
            print("模型加载成功（使用非严格模式）")
            return model
        except Exception as e2:
            print(f"模型修复失败: {e2}")
            return None

def process_audio_chunks(model, waveform, chunk_size, overlap):
    """分块处理长音频"""
    total_length = waveform.shape[1]
    hop_size = chunk_size - overlap
    
    output = torch.zeros_like(waveform)
    weights = torch.zeros(1, total_length).to(waveform.device)  # 用于累积窗口权重
    window = torch.hann_window(chunk_size).to(waveform.device)
    
    num_chunks = (total_length - overlap + hop_size - 1) // hop_size
    print(f"分为 {num_chunks} 块处理...")
    
    for i, start in enumerate(range(0, total_length, hop_size)):
        end = min(start + chunk_size, total_length)
        chunk_len = end - start
        
        # 提取块
        chunk = waveform[:, start:end]
        
        # Padding
        padded = False
        if chunk_len < chunk_size:
            chunk = F.pad(chunk, (0, chunk_size - chunk_len))
            padded = True
        
        # 推理
        chunk_input = chunk.unsqueeze(0).to(config.device)
        with torch.no_grad():
            separated_chunk = model(chunk_input)
        
        separated_chunk = separated_chunk.squeeze(0).cpu()
        
        # 处理模型输出维度
        if separated_chunk.dim() == 1:
            separated_chunk = separated_chunk.unsqueeze(0)
        
        # 裁剪padding
        if padded:
            separated_chunk = separated_chunk[:, :chunk_len]
        
        # 应用窗口函数
        win = window[:chunk_len]
        separated_chunk = separated_chunk * win
        
        # 累加到输出
        output[:, start:end] += separated_chunk
        weights[:, start:end] += win
        
        # 显示进度
        if (i + 1) % max(1, num_chunks // 10) == 0 or i == num_chunks - 1:
            progress = (i + 1) / num_chunks * 100
            print(f"处理进度: {progress:.1f}%")
    
    # 归一化（除以窗口权重）
    weights = torch.clamp(weights, min=1e-8)
    output = output / weights
    
    return output

def process_audio(model, audio_path):
    """处理音频"""
    filename = Path(audio_path).name
    print(f"正在加载: {filename}")
    
    try:
        # 加载音频
        waveform, sr = torchaudio.load(audio_path)
        print(f"原始采样率: {sr}Hz, 时长: {waveform.shape[1]/sr:.2f}秒")
        
        # 重采样
        if sr != config.sample_rate:
            print(f"重采样到 {config.sample_rate}Hz...")
            resampler = torchaudio.transforms.Resample(sr, config.sample_rate)
            waveform = resampler(waveform)
        
        # 转换为立体声（2通道）
        if waveform.shape[0] == 1:
            print("转换为立体声...")
            waveform = waveform.repeat(2, 1)
        elif waveform.shape[0] > 2:
            print("提取前两个通道...")
            waveform = waveform[:2, :]
        
        # 归一化输入
        max_val = torch.max(torch.abs(waveform))
        if max_val > 1e-8:
            waveform = waveform / max_val * 0.95
        
        # 分块处理
        print("正在分离吉他音轨...")
        chunk_size = config.sample_rate * 10  # 10秒块
        overlap = config.sample_rate * 2      # 2秒重叠
        
        separated = process_audio_chunks(model, waveform, chunk_size, overlap)
        
        # 反归一化输出
        separated = separated * max_val / 0.95
        
        # 限制幅度，防止削波
        separated = torch.clamp(separated, -1.0, 1.0)
        
        return separated
        
    except RuntimeError as e:
        if "out of memory" in str(e):
            print("显存不足！尝试减小 chunk_size")
        else:
            print(f"运行时错误: {e}")
        return None
    except Exception as e:
        print(f"处理错误: {e}")
        import traceback
        traceback.print_exc()
        return None

def main():
    print("=" * 50)
    print("      吉他音轨分离工具")
    print("=" * 50)
    
    # 创建必要的目录
    MUSIC_DIR.mkdir(parents=True, exist_ok=True)
    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
    
    print(f"音乐文件夹: {MUSIC_DIR}")
    print(f"输出文件夹: {OUTPUT_DIR}")
    
    # 选择音频文件
    audio_file = select_audio_file()
    if not audio_file:
        return

    # 加载模型
    model = load_model()
    if model is None:
        return

    # 处理音频
    separated_waveform = process_audio(model, audio_file)
    
    if separated_waveform is not None:
        # 清理文件名
        original_name = Path(audio_file).stem
        sanitized_name = sanitize_filename(original_name)
        output_filename = f"{sanitized_name}_guitar.wav"
        output_path = OUTPUT_DIR / output_filename
        
        print(f"保存结果: {output_path}")
        
        try:
            # 确保张量在CPU上且是float32类型
            separated_waveform = separated_waveform.cpu().float()
            
            # 保存为 WAV 文件
            torchaudio.save(
                str(output_path), 
                separated_waveform, 
                config.sample_rate,
                encoding="PCM_S",  # 16-bit PCM
                bits_per_sample=16
            )
            print("✓ 完成！")
            
            # 显示输出信息
            print(f"\n输出信息:")
            print(f"  - 文件: {output_filename}")
            print(f"  - 采样率: {config.sample_rate}Hz")
            print(f"  - 时长: {separated_waveform.shape[1]/config.sample_rate:.2f}秒")
            print(f"  - 通道数: {separated_waveform.shape[0]}")
            print(f"  - 幅度范围: [{separated_waveform.min():.3f}, {separated_waveform.max():.3f}]")
            
        except Exception as e:
            print(f"✗ 保存失败: {e}")
            import traceback
            traceback.print_exc()
    else:
        print("✗ 处理失败")

if __name__ == "__main__":
    main()
```

## __init__.py
文件路径: test\__init__.py

```py

```

